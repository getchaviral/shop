{
  "name": "traditional-shop-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "client": "npm run dev --prefix frontend",
    "server": "npm run server --prefix backend",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
